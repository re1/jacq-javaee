- Synchronize only Tables and Routines, except tbl_botanical_object and tbl_derivative_vegetative (do not drop yet!, set SQL_MODE parameter to empty!)
- Synchronize all other changes (again except tbl_botanical_object and tbl_derivative_vegetative)
- Insert derivative_type entries (1 && 2)
- materialize migration view for derivatives (mig_derivative) INSERT INTO tbl_derivative SELECT * FROM mig_derivative;
- materialize migration view for vegetatives (mig_vegetative) INSERT INTO tbl_vegetative SELECT * FROM mig_vegetative;
- synchronize changes in tbl_botanical_object and tbl_derivative_vegetative, ignore mig_derivative & mig_vegetative (becomes invalid)
- Create View for BuGa-Data import (CREATE VIEW buga_dataImport AS SELECT * FROM `BuGa_Data`.`view_importData`)
- Create organisation entries for buga-data import (Import tblOrganisation.sql)
- Add dataimport / dataimport user
- Add user for mknaack
- Run actual dataimport routine
